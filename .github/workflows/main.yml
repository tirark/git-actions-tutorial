name: MiPrimerWorkflow
on: [push, pull_request]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Usar Secret
        run: echo "La API_KEY es ${{ secrets.API_KEY }}"
        
        
